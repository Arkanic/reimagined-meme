import constants from "../../../../shared/constants";
import * as serialized from "../../../../shared/types/serializedData";
import * as assets from "../../assets";

export function renderPlayer(ctx:CanvasRenderingContext2D, canvas:HTMLCanvasElement, me:serialized.Player, player:serialized.Player):void {
    const {x, y} = player;
    
}